"""Minimal package marker for the browser demo. Only rubric.py, provenance.py and
validators.py are copied verbatim from the upstream repo (commit c9083a6) -- runner.py,
abstain.py, tracking.py and cli.py are NOT needed for single-record admission via
build_payload() and are intentionally left out of this demo bundle."""
